<h1>プロフィール詳細</h1>
<div id="conversations"><h2><%= @user.name %></h2></div>

<% if @conver_id == true %>
  <p><%= link_to "会話へ", conversation_path(@conversation) %></a>
<% else %>
  <%= render "form" %>
<% end %>

<% if current_user.admin? %>
  <%= link_to "ユーザー一覧に戻る", users_path %>
<% else %>
  <%= link_to "HOME", home_path %>
<% end %> 