<% if user.avatar.present? %>
  <%= link_to image_tag(user.avatar.variant(resize: "100x100"), class: "icon"), user_path(user) %>
<% else %>
  <%= link_to image_tag("icon.gif", size: "100x100", class: "icon"), user_path(user) %>
<% end %>