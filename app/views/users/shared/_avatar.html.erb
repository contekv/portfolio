<% if user.avatar.present? %>
  <%= link_to image_tag(user.avatar.variant(resize: "50x50")), user_path(user) %>
<% else %>
  <%= link_to image_tag("default_user.png", size: "50x50"), user_path(user) %>
<% end %>