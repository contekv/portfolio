<header>
  <% if current_user.admin? %>
    <%= link_to image_tag("Logo.gif", id: "logoimg", alt: "MediRoom"), admin_path(current_user) %>
  <% else %>
    <%= link_to image_tag("Logo.gif", id: "logoimg", alt: "MediRoom"), home_path(current_user) %>
  <% end %>
</header>